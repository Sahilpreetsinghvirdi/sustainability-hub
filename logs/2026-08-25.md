# 25 aug 2026

## the night of the build (12:00 AM – 1 AM)

continued from yesterday's crash. exact timeline:

- **~12:00–12:30 AM** — figured out WHY rustc kept dying with STATUS_STACK_BUFFER_OVERRUN
  on random crates: my C: drive is 100% FULL (0 gb free!!) so windows pagefile cant grow,
  memory commit was capped at ~17gb with only ~2.2 free. big compiler processes get killed
  by windows. its not a code bug at all, my laptop is just choking
- cleaned old caches (dead .rustup/.cargo on C:, temp files) — freed like 0.7gb only.
  pointed build TMP to D:\rusttmp and ran cargo single threaded (-j1). slower but works
- **12:00:13 AM** — MSI INSTALLER BUILT!!! `Sustainability Hub_1.0.0_x64_en-US.msi` (3.8 mb)
- **12:00:17 AM** — exe also built: sustainability-hub-desktop.exe (10.4 mb)
- **~12:05 AM** — updated start-app.vbs to launch the REAL exe instead of edge. native
  window confirmed running ("Sustainability Hub" own taskbar icon, no browser AT ALL)
- so now: double click shortcut → backend starts hidden → real desktop app opens.
  actual installable software now, msi can go on any windows pc

## api key day (morning)

- got FREE gemini api key from aistudio.google.com/apikey (takes 2 mins, no card needed)
- put it in backend/.env (gitignored, key never goes to github)
- restarted backend → status endpoint shows ai_configured: true ✓
- first analyze test FAILED — gemini-2.0-flash model is retired! the API error message
  itself told me to use gemini-3.6-flash lol. changed model in .env + config.py +
  .env.example
- second test timed out at 90s → raised AI_TIMEOUT_SECONDS to 180 in .env
- **FINAL TEST PASSED** — sent image through /api/v1/waste/analyze, got back full JSON:
  summary, hazard level/score/toxins, materials list, recomendations, enviromental impact,
  decomposition time, processing_time_ms: 9698 (~10 seconds!)
- funny part: i tested with the app logo png and the AI correctly said "this is not
  garbage" and asked for a real waste photo. good sign it wont make stuff up

## what actually works end to end now

photo/webcam → backend → gemini vision AI → structured breakdown → charts in the
native desktop app. THE core feature is done and real.

## next steps
- test with actual garbage photos from home (kitchen waste, plastic wrappers, e-waste)
- store analysis history in postgres
- mobile app fixes still pending
- maybe add hindi language output option for judges demo?

## late night bugfix session (12:30 – 1:00 AM)

two bugs found while testing, both fixed and pushed:

- **12:45 AM** — pie chart colors all looked the same!! spent ages checking recharts
  versions etc, then realised the REAL bug: i was coloring slices by WASTE CATEGORY,
  so if a photo has 3 plastic items they all got the same blue. now every material
  gets its own color from a 12-color palette + slices sorted biggest first + % labels
  right on the chart + white borders between slices. rebuilt exe (2m30s incremental)
- **~1:00 AM** — opening the app flashed TWO black command prompts. traced it to the
  old vbs launcher: WshShell.Exec ALWAYS pops a console window, can't be hidden, its
  a known vbs limitation. rewrote launcher in python (start-app.pyw) with windowless
  pythonw.exe. first attempt still failed silently — pythonw hides errors AND
  subprocess.Popen dies under pythonw. final fix: ctypes ShellExecuteW (the literal
  API windows uses when you double click things). app opens in 2 seconds, ZERO
  console windows. shortcut now points to pythonw, deleted old vbs
- learned today: silent failures are the worst. added logging to start-app.log so
  next time the launcher misbehaves i can actually see why

## Session 2026-08-25 (later) � Memory System v1.2.0
- Problem: navigating away mid-analysis lost everything; pages showed hardcoded demo data.
- Built: localStorage memory layer (store.ts) -> scan history w/ canvas thumbnails (cap 25), auto-restore last scan, navigation-safe in-flight analysis (module pub/sub), organic materials auto-linked to Food Waste (grams from % of ~1.2kg pile, 0.0025 kgCO2/g).
- All pages now hydrate real persisted data: Dashboard (weekly carbon / monthly energy / daily waste + real streaks), Carbon receipts, Energy bills+appliances, Food Waste logs/streak/30-day grid. Empty states replace fake seeds.
- Fixed AnalysisResults literal ${hs.text} class bug; btn-primary dark fix shipped earlier today.
- Released: tag v1.2.0 + GitHub Release w/ Sustainability-Hub-v1.2.0-setup.msi (3.8 MB). Commits db33c93, efff72e.
 - Secret file .ai/WEBSITE_HANDOFF.md written for companion-site AI (gitignored, local only).

## Session 2026-08-25 (v1.3.0) — Polish & Safety
 - AgriSense + WasteAnalyzer: history moved to thin bottom-right drawer (drawer-grid 0fr→1fr, scroll-x-contained), saved reports open as dedicated page (image+results only) with Back button.
 - Saved image quality fixed: 1600px 0.92 JPEG via createImageBitmap(imageOrientation:'from-image') for exact orientation, plus 260px thumb; stored as `image` + `thumb` in history.
 - Global polish: pageIn 0.28s on route change, drawer-grid transitions, toastIn for bottom-right toasts, overflow-x-hidden everywhere — no more sideways page drag (screenshot bug).
 - Auto-open results: wasAnalyzingRef → smooth scroll to resultsRef on fresh completion (120ms); if on another page, AppLayout toast “View results →” (App.tsx) instead.
 - AgriSense safety: backend prompt now forces harmful 0/100 for garbage/plastic/municipal waste (agri_analysis.py), _normalize overrides neutral ≤15 to harmful; frontend effectiveSuitability() color-grades bar (bg-error for harmful) + “DO NOT APPLY” banner + pulsing icon.
 - Settings: new backend endpoint GET/POST /api/v1/settings/ai (settings_ai.py) masked keys, .env upsert, provider/model switch; frontend Settings → AI Configuration tab (Gemini/OpenAI keys with Eye toggle, masked current, Clear, Save). Other tabs made functional: Profile/Household/Notifications persist via store.ts (sh_profile etc.), Data & Privacy replaces Cloud Sync (clear histories, export JSON, reset all). Sidebar now shows live profile initials/name via profile-updated event.
